# NutriAdvisor

**AI-помощник по питанию** — мобильное веб-приложение (PWA) для персонализированного анализа и коррекции питания.

Приложение помогает скорректировать питание исходя из ваших целей (похудение, набор массы, улучшение сна и др.), анализирует съеденные продукты через текст или фото, даёт рекомендации через AI-ассистента и ведёт журнал питания.

> **Бессерверный режим** — все данные и история хранятся локально на устройстве. API ключи для LLM также хранятся только у вас. Никаких серверных компонентов.

---

## Возможности

- **Конфигурируемые LLM-провайдеры** — OpenAI, OpenRouter, Alibaba DashScope, Ollama (локальный), произвольные. Добавляйте, редактируйте, удаляйте провайдеров и переключайтесь между ними.
- **10 целей питания** — здоровье, похудение, набор массы, набор мышц, сушка, улучшение сна, самочувствие, бодрость ума, энергия, поддержание веса.
- **Журнал питания** — ручной ввод продуктов с описанием и весом порции, либо анализ по фото (камера или галерея). AI оценивает калорийность и БЖУ.
- **AI-чат ассистент** — диалоговый интерфейс с контекстными рекомендациями. Понимает контекст: ежедневный анализ, планирование меню, мотивация, обучение, экстренная поддержка и т.д.
- **Профиль пользователя** — параметры тела, уровень активности, ограничения в питании, медицинские заметки. Рекомендации адаптируются под профиль.
- **Дашборд** — сводка калорий и макронутриентов за день, прогресс по целям, быстрые действия.
- **PWA** — устанавливается на Android через браузер (Добавить на главный экран), работает как нативное приложение.

---

## Стек технологий

| Слой | Технология |
|---|---|
| Фреймворк | Next.js 16 (App Router), React 19, TypeScript |
| UI | Tailwind CSS 4, shadcn/ui |
| Локальное хранилище | Dexie.js (IndexedDB) |
| Состояние | Zustand |
| Иконки | Lucide React |
| Развертывание | PWA (manifest + иконки) |

---

## Установка и запуск

### Требования

- **Node.js** 18+ или **Bun** 1.0+
- **npm**, **yarn** или **bun**

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/NikasAl/NutriAdvisor.git
cd NutriAdvisor
```

### 2. Установите зависимости

```bash
npm install
# или
bun install
```

### 3. Запустите в режиме разработки

```bash
npm run dev
# или
bun run dev
```

Приложение будет доступно по адресу **http://localhost:3000**.

### 4. Сборка для продакшена

```bash
npm run build
npm run start
```

---

## Первый запуск

1. Откройте приложение в браузере.
2. Перейдите на вкладку **Настройки** (шестерёнка в нижнем меню).
3. Нажмите **Добавить** провайдер — выберите тип (OpenAI, OpenRouter и т.д.) и укажите ваш API ключ.
4. Провайдер с ключом автоматически станет активным.
5. Заполните **Профиль** — укажите имя, параметры тела, выберите цели.
6. Всё готово — добавляйте приём пищи в журнале или общайтесь с AI в чате.

---

## Структура проекта

```
NutriAdvisor/
├── public/                    # Статические файлы
│   ├── manifest.json           # PWA манифест
│   ├── icon-192.png            # Иконка 192x192
│   ├── icon-512.png            # Иконка 512x512
│   └── icon.svg                # Векторная иконка
├── src/
│   ├── app/
│   │   ├── layout.tsx          # Корневой layout (PWA meta)
│   │   ├── page.tsx            # Главная страница (роутинг по вкладкам)
│   │   └── globals.css         # Глобальные стили (Tailwind)
│   ├── components/
│   │   ├── app/                # Экраны приложения
│   │   │   ├── BottomNav.tsx         # Нижняя навигация
│   │   │   ├── DashboardPanel.tsx   # Главная — дашборд
│   │   │   ├── FoodJournalPanel.tsx  # Журнал питания
│   │   │   ├── ChatAssistantPanel.tsx # AI-чат
│   │   │   ├── ProfilePanel.tsx      # Профиль пользователя
│   │   │   └── SettingsPanel.tsx     # Настройки провайдеров
│   │   └── ui/                 # shadcn/ui компоненты
│   ├── hooks/                  # React-хуки
│   ├── lib/
│   │   ├── types.ts            # TypeScript типы
│   │   ├── db.ts               # Dexie.js (IndexedDB) схема
│   │   ├── llm-client.ts       # Универсальный LLM-клиент
│   │   ├── prompts.ts          # Система промптов питания
│   │   └── utils.ts            # Утилиты
│   └── store/
│       └── useAppStore.ts       # Zustand store
├── components.json              # Конфигурация shadcn/ui
├── eslint.config.mjs            # ESLint
├── next.config.ts               # Конфигурация Next.js
├── package.json
├── postcss.config.mjs           # PostCSS (Tailwind)
├── tsconfig.json                # TypeScript
└── .gitignore
```

---

## Как это работает

### Хранение данных
Все данные хранятся в **IndexedDB** браузера через библиотеку Dexie.js. 5 таблиц:
- `providers` — конфигурации LLM-провайдеров и API ключи
- `userProfile` — профиль пользователя (параметры, цели, ограничения)
- `foodEntries` — записи о приёмах пищи
- `chatSessions` — сессии чата с AI
- `chatMessages` — сообщения чата

### LLM интеграция
Прямые HTTP-запросы к API провайдеров с ключами пользователя (формат OpenAI-compatible). Поддерживаются любые провайдеры с совместимым API:
- **OpenAI** (GPT-4o, GPT-4o-mini, ...)
- **OpenRouter** (DeepSeek, Claude, Llama, ...)
- **Alibaba DashScope** (Qwen, ...)
- **Ollama** (локальные модели, без интернета)
- Любые другие (вставьте свой Base URL)

### Контекстные промпты
Система автоматически определяет тему сообщения (ежедневный анализ, планирование, мотивация, обучение, проблемы со сном и т.д.) и подбирает соответствующий системный промпт, обогащённый данными профиля пользователя.

---

## Лицензия

MIT
